snapshots:
  - name: new_snapshot_customers
    relation: source('bootcamp', 'js_raw_customers') # source('my_source', 'my_table') or ref('my_model')
    config:
      strategy: timestamp # timestamp | check
      unique_key: id
      #check_cols: [column_name] | all
      updated_at: last_updated_dt
      #invalidate_hard_deletes: true | false
      #snapshot_meta_column_names: dictionary